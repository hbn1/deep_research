# DeepResearch 多智能体深度研究系统期末项目报告

## 摘要

本项目设计并实现了一个 AI 相关的深度研究助手 DeepResearch。系统面向复杂调研、知识问答和报告生成场景，采用“意图分流 + 多智能体协作 + 检索增强生成 + 会话记忆”的架构。用户提交问题后，系统会先判断任务复杂度：简单问题由轻量模型快速回答，复杂问题则进入 LangGraph 编排的研究链路，依次完成问题规划、网页检索、本地知识库检索、证据审查、分析反思和报告写作。系统提供 FastAPI 后端接口、Vue 前端交互页面和命令行入口，并支持 Redis、PostgreSQL、Milvus 等基础设施增强记忆和 RAG 能力。

项目的核心目标不是简单调用大模型生成文本，而是构建一个具备任务拆解、信息检索、证据组织、结果追溯和跨轮记忆能力的 AI 应用原型。通过该项目，可以展示大模型应用开发中多智能体编排、检索增强生成、状态管理、流式响应、前后端集成和工程化部署等关键技术。

## 一、项目背景与意义

随着大语言模型的发展，AI 助手已经能够完成问答、摘要、写作和代码生成等任务。但在期末调研、课程报告、企业竞品分析等复杂场景中，单次提示词直接生成答案存在几个明显问题：

1. 缺少任务拆解，复杂问题容易遗漏关键维度。
2. 缺少实时资料，模型知识可能过期。
3. 缺少来源引用，结论难以验证。
4. 缺少多轮记忆，用户偏好和上下文难以延续。
5. 缺少执行过程可视化，用户无法判断系统卡在哪一步。

DeepResearch 针对上述问题，将传统问答式 AI 助手升级为“研究工作流”。系统使用多智能体分别承担规划、检索、证据判断、分析和写作任务，让模型输出从“直接回答”变成“基于证据的结构化研究结果”。这符合当前 AI 应用从单模型调用向 Agentic Workflow 发展的趋势。

## 二、需求分析

### 2.1 功能需求

系统需要满足以下功能：

- 用户可以通过前端页面、API 或命令行提交问题。
- 系统能够识别简单问答和复杂研究任务。
- 对复杂任务自动拆分子问题和检索计划。
- 支持联网搜索，获取较新的网页信息。
- 支持本地知识库检索，提高私有资料利用率。
- 支持短期和长期记忆，用于多轮对话和个性化回答。
- 输出 Markdown 格式报告，并尽量附带来源引用。
- 前端能够展示会话历史、阶段进度和最终结果。
- 后端提供健康检查、同步调用和流式调用接口。

### 2.2 非功能需求

- 可扩展性：新节点、新搜索后端和新记忆后端应能相对容易接入。
- 可维护性：后端、智能体、记忆、RAG、前端模块边界清晰。
- 鲁棒性：外部服务不可用时尽量降级，不直接导致整个系统崩溃。
- 可观测性：通过日志和 SSE 事件展示当前执行阶段。
- 安全性：支持 API Key 鉴权和基础限流，避免接口被滥用。

## 三、总体设计

### 3.1 系统架构

系统整体分为五层：

```text
用户交互层
  - Vue 前端
  - 命令行 CLI
  - HTTP API

服务接口层
  - FastAPI
  - 路由、请求模型、响应模型
  - API Key 鉴权和限流中间件

智能体编排层
  - LangGraph StateGraph
  - 意图识别、规划、检索、分析、写作等节点

能力增强层
  - Web 搜索
  - 本地 RAG
  - 短期/长期记忆
  - Checkpointer

基础设施层
  - DashScope 大模型
  - Redis
  - PostgreSQL
  - Milvus
  - Docker Compose
```

### 3.2 模块划分

项目主要代码位于 `app` 和 `front/agent_front`：

- `app/app_main.py`：FastAPI 服务创建入口。
- `app/backend/router`：定义健康检查与调研接口。
- `app/backend/service/workflow_service.py`：负责延迟初始化 LangGraph App、模型、记忆系统和 checkpointer。
- `app/mult_agents/graph.py`：定义 LangGraph 节点与条件路由。
- `app/mult_agents/nodes`：实现各类智能体节点。
- `app/mult_agents/search.py`：统一搜索入口，处理缓存、查询改写、并发搜索和重排。
- `app/mult_agents/rag`：本地知识库的入库与检索。
- `app/mult_agents/memory`：短期记忆、长期记忆、记忆抽取和注入。
- `front/agent_front/src/App.vue`：前端主要交互界面。

### 3.3 数据流

一次复杂研究请求的主要数据流为：

1. 用户在前端输入问题。
2. 前端调用 `/api/v1/research/stream`。
3. 后端 `WorkflowService` 构造初始状态。
4. 记忆模块根据 `user_id`、`thread_id`、`tenant_id` 召回上下文。
5. LangGraph 执行多智能体节点。
6. Web 与本地知识库返回证据材料。
7. 分析节点判断是否需要补搜。
8. 写作节点生成最终 Markdown 报告。
9. 后端流式返回阶段事件和最终结果。
10. 记忆模块保存本轮对话和抽取出的事实。

## 四、关键技术实现

### 4.1 意图分流

系统首先执行 `intent` 节点，对用户问题进行分类。简单问候、翻译、时间、定义类问题可以直接进入 `direct_answer` 节点，避免完整研究链路带来的延迟和成本。复杂问题进入多智能体链路。

这种设计的优点是：

- 降低简单问题的响应时间。
- 节省大模型调用成本。
- 将复杂任务交给更完整的研究流程处理。

### 4.2 LangGraph 多智能体编排

项目使用 LangGraph 的 `StateGraph` 构建状态机。每个节点读取并更新统一的 `ResearchState`，节点之间通过条件边控制流程。

复杂任务链路为：

```text
intent -> plan -> web_search/local_rag -> deep_dive -> analyze
       -> reflect 或 prune_context -> write -> memory_reflect -> END
```

其中：

- `plan` 负责输出研究目标、子问题、检索计划和报告大纲。
- `web_search` 负责联网检索与资料筛选。
- `local_rag` 负责本地知识库召回。
- `deep_dive` 负责证据可靠性和冲突判断。
- `analyze` 负责形成核心发现和缺口判断。
- `reflect` 负责在资料不足时生成补充检索计划。
- `write` 负责生成最终报告。
- `memory_reflect` 负责沉淀可复用记忆。

### 4.3 检索增强生成

项目同时实现了 Web 搜索和本地 RAG 两类信息来源。

Web 搜索模块支持：

- 多搜索后端配置。
- 查询改写。
- 并发检索。
- URL 去重。
- 网页正文抓取。
- 相关性、权威性、新鲜度综合重排。
- Redis 或内存缓存。

本地 RAG 模块支持：

- TXT、Markdown、PDF、DOCX 和代码文件入库。
- 文档切分和 chunk 管理。
- Milvus 向量检索。
- BM25 稀疏检索。
- RRF 融合。
- DashScope rerank 或词项重叠 fallback。

通过双源检索，系统既可以利用互联网信息，也可以利用项目或企业内部资料。

### 4.4 记忆系统

项目实现了两套记忆方案：

- v2 记忆：Redis 短期记忆 + PostgreSQL 长期结构化存储 + Milvus 向量索引。
- v1 fallback：SQLite/本地记忆管理器。

记忆流程分为召回和持久化：

- 召回阶段：根据当前问题、用户、线程和租户读取最近消息、摘要和长期记忆，并注入模型提示词。
- 持久化阶段：将用户问题和回答写入短期记忆，再抽取事实、偏好、约束和流程模式写入长期记忆。

这种设计让系统具备跨轮次理解能力。例如用户偏好、研究背景和历史任务可以在后续对话中被重新利用。

### 4.5 FastAPI 后端

后端提供三个主要接口：

- `GET /health`：健康检查。
- `POST /api/v1/research/run`：同步执行研究任务。
- `POST /api/v1/research/stream`：通过 SSE 返回阶段进度和最终报告。

后端还实现了：

- API Key 鉴权中间件。
- 滑动窗口限流中间件。
- CORS 配置。
- `WorkflowService` 延迟初始化，避免启动时立即连接所有外部服务。
- v2 记忆失败时自动 fallback 到 v1 记忆。

### 4.6 前端交互

前端使用 Vue 3 + TypeScript + Vite 开发，主要功能包括：

- 多会话管理。
- 本地存储会话历史。
- 推荐起始问题。
- 用户 ID 与租户 ID 设置。
- 流式读取 SSE 响应。
- 阶段状态展示。
- Markdown 简易渲染。
- 生成中断控制。

前端定位是一个“研究工作台”，用户可以直接看到系统当前处于规划、检索、分析还是写作阶段。

## 五、核心算法与流程

### 5.1 复杂任务执行算法

```text
输入：用户问题 query
输出：Markdown 研究报告 final

1. 初始化 ResearchState。
2. 召回用户短期与长期记忆。
3. 判断 query 是 direct 还是 multiagent。
4. 若为 direct，调用轻量回答节点并结束。
5. 若为 multiagent，生成研究计划。
6. 根据计划并行执行 Web 搜索与本地 RAG。
7. 合并证据，过滤低质量来源。
8. 判断证据是否足够。
9. 若缺口权重较高且未超过最大迭代次数，生成补搜计划并重复检索。
10. 裁剪上下文，生成最终报告。
11. 抽取本轮记忆并持久化。
```

### 5.2 检索结果重排思路

Web 检索结果综合考虑：

- 关键词相关性。
- 来源域名权威性。
- 发布时间新鲜度。
- 摘要信息完整度。

本地 RAG 结果综合考虑：

- 向量相似度。
- BM25 关键词匹配。
- RRF 排名融合。
- DashScope rerank 或本地重排 fallback。

### 5.3 记忆召回排序

长期记忆排序不仅依赖文本匹配，还引入业务分数，包括：

- 记忆重要性。
- 最近访问时间。
- 召回次数。
- 记忆类型权重。

这样可以避免长期记忆只按关键词匹配，提升对用户偏好和重要事实的保留能力。

## 六、系统实现亮点

### 6.1 轻重链路分离

系统不会对所有问题都运行复杂多智能体流程，而是先进行意图分流。简单问题快速回答，复杂问题才启用完整研究链路。这体现了工程中的成本控制意识。

### 6.2 状态驱动的工作流

所有节点围绕 `ResearchState` 交换数据，避免不同 agent 之间直接耦合。新增字段或节点时，只需扩展状态结构和图边关系。

### 6.3 双源证据融合

Web 搜索适合获取实时信息，本地 RAG 适合利用私有资料。系统将两者并行召回，再统一交给证据判断和写作节点处理。

### 6.4 记忆 fallback 设计

系统优先使用 Redis、PostgreSQL、Milvus 组成的增强记忆系统；当外部服务不可用时，可以降级到本地记忆管理器。这提高了课程演示和本地开发的成功率。

### 6.5 前后端流式体验

前端通过 SSE 接收阶段事件，用户可以看到系统正在执行的节点，而不是等待一个长时间无反馈的接口返回。

## 七、测试与验证

项目包含两个主要测试脚本。

### 7.1 记忆系统测试

`tests/test_memory_system.py` 覆盖了：

- 数据库 DDL 基础检查。
- 记忆类型与实体结构。
- 统一存储辅助函数。
- 记忆注入格式化。
- 规则抽取器 fallback。
- 短期记忆服务。
- 记忆编排器逻辑。
- 并发持久化安全性。
- SQLite 多轮消息能力。
- 插入、读取、冲突解决等性能测试。
- 空内容、大文本、空 key 等边界情况。

### 7.2 多轮对话测试

`tests/test_multiturn.py` 使用多个场景验证：

- 用户偏好积累与召回。
- 研究主题连续性。
- 跨 session 记忆召回。
- 混合主题隔离能力。

### 7.3 前端与接口验证

前端可通过 `npm run build` 做类型检查与构建验证。后端可通过 `/health` 检查服务状态，并通过 `/api/v1/research/run` 或 `/api/v1/research/stream` 验证完整链路。

## 八、部署说明

项目支持本地运行和 Docker Compose 运行。

本地开发时：

1. 复制 `.env.example` 为 `.env`。
2. 填写 `DASHSCOPE_API_KEY` 和搜索 API Key。
3. 安装 Python 与 Node 依赖。
4. 启动 FastAPI 后端。
5. 启动 Vue 前端。

Docker Compose 方式会同时拉起后端、Redis、PostgreSQL、Milvus、etcd 和 MinIO，适合演示完整基础设施版本。

## 九、遇到的问题与解决方案

### 9.1 外部依赖较多

系统依赖模型 API、搜索 API、Redis、PostgreSQL 和 Milvus。为降低启动失败风险，项目实现了延迟初始化和 fallback 机制，使部分能力不可用时仍可运行核心问答链路。

### 9.2 复杂链路响应时间长

完整研究链路包含多次模型调用和多源检索。项目通过意图分流、轻量模型、搜索缓存、并发检索和最大迭代次数限制降低延迟。

### 9.3 引用可靠性控制

大模型可能生成不存在的引用编号。项目在写作节点中维护合法 source id，并对最终内容中的引用进行校验和修正，尽量保证引用可追溯。

### 9.4 上下文过长

检索结果、证据池和对话历史可能导致提示词过长。项目通过 `prune_context`、摘要、证据筛选和 source index 裁剪控制上下文规模。

## 十、不足与改进方向

当前项目仍有一些可以继续完善的地方：

- 前端文档上传和知识库管理功能还不完整。
- 引用质量仍依赖检索结果和模型遵循指令的能力。
- API 鉴权较简单，适合课程项目和本地演示，生产环境需要用户级权限系统。
- 搜索后端在不同网络环境下稳定性不同，需要更完整的错误恢复策略。
- 自动评测体系还可以增强，例如增加答案事实性、引用命中率和报告结构评分。
- 部分旧源码注释或界面文案存在编码显示异常，后续应统一 UTF-8 编码并清理无用模板文件。

## 十一、课程收获

通过本项目，可以系统性学习和实践以下内容：

- 大语言模型应用不只是调用 API，还需要合理的工程架构。
- LangGraph 适合描述多阶段、可条件跳转的 Agent 工作流。
- RAG 能够降低模型幻觉，但需要检索、过滤、重排和引用校验配合。
- 记忆系统需要区分短期上下文和长期知识，且要考虑租户隔离和召回排序。
- 流式输出可以显著提升复杂 AI 任务的用户体验。
- 一个可演示的 AI 项目需要同时考虑前端、后端、配置、部署和测试。

## 十二、总结

DeepResearch 实现了一个完整的 AI 多智能体深度研究系统原型。它通过意图分流提升效率，通过 LangGraph 实现多智能体协作，通过 Web 搜索和本地 RAG 增强事实依据，通过记忆系统提升多轮对话能力，并通过 FastAPI 与 Vue 构建可交互应用。项目整体覆盖了大模型应用开发中的模型调用、Agent 编排、检索增强、状态管理、流式接口、前后端集成和部署测试等关键环节，能够作为 AI 相关期末大作业的完整提交成果。

后续如果继续迭代，可以重点加强知识库管理、引用验证、评测体系和前端产品体验，使其从课程原型进一步发展为更实用的研究工作台。
